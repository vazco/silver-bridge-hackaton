# AGENTS.md

## Cursor Cloud specific instructions

This is a freshly-initialized hackathon repository (`silver-bridge-hackaton`). As of initial setup, it contains only a README with the project title and no application code, dependencies, or services.

### Current state
- **No application code** — no source files, no entry points
- **No dependencies** — no `package.json`, `requirements.txt`, `pyproject.toml`, or similar
- **No build system** — no `Makefile`, webpack config, or build scripts
- **No tests** — no test framework or test files
- **No services** — nothing to start or run

### For future agents
When code is added to this repository, update this section with:
- How to install dependencies
- How to run the application in development mode
- How to run tests and lint checks
- Any non-obvious environment setup steps or gotchas
